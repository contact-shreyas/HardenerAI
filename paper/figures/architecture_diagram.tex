% Architecture diagram for paper (embedded in main.tex or separate)
% This is a placeholder - to be included as Figure 1 in the paper

\begin{figure}[t]
\centering
\begin{tikzpicture}[
  node distance=1.2cm and 1.5cm,
  box/.style={rectangle, draw, minimum width=2.5cm, minimum height=0.8cm, align=center, font=\small},
  arrow/.style={->, >=stealth, thick}
]

% Input layer
\node[box] (configs) {Config Files\\{\tiny Nginx, Docker, K8s}};
\node[box, right=of configs] (scanners) {External Scanners\\{\tiny Trivy, kube-bench}};

% Processing layer
\node[box, below=of configs] (ingestion) {Ingestion \&\\Normalization};
\node[box, below=of ingestion] (dedup) {Deduplication\\{\tiny 84\% noise reduction}};
\node[box, below=of dedup] (scoring) {Risk Scoring\\{\tiny CVSS-inspired}};
\node[box, below=of scoring] (patching) {Patch Generation\\{\tiny Minimal-change diffs}};
\node[box, below=of patching] (validation) {Validation \&\\Rollback};

% Output layer
\node[box, right=2cm of validation] (reports) {Reports\\{\tiny JSON + Markdown}};
\node[box, above=of reports] (dashboard) {Dashboard\\{\tiny Next.js UI}};

% Arrows
\draw[arrow] (configs) -- (ingestion);
\draw[arrow] (scanners) -- (ingestion);
\draw[arrow] (ingestion) -- (dedup);
\draw[arrow] (dedup) -- (scoring);
\draw[arrow] (scoring) -- (patching);
\draw[arrow] (patching) -- (validation);
\draw[arrow] (validation) -- (reports);
\draw[arrow] (reports) -- (dashboard);

\end{tikzpicture}
\caption{System Architecture Pipeline: 7-stage hardening flow from config ingestion to reporting}
\label{fig:architecture}
\end{figure}

% TODO: Compile this separately or embed in main.tex after adding \usepackage{tikz}
% For paper, add \usepackage{tikz} to preamble
