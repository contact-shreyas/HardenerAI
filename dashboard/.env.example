# Dashboard Environment Variables

# Enable UI Apply feature (allows applying patches from web UI)
# WARNING: Only enable in trusted environments
HARDENER_UI_APPLY=0

# Optional: Override workspace root path
# HARDENER_WORKSPACE=/path/to/workspace
